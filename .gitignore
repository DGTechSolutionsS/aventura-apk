node_modules/
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/capacitor-cordova-android-plugins/
# NUNCA commitar a chave de assinatura:
aventura-release.jks
android/keystore.properties
*.jks
*.keystore
# segredo do OTA (upload de bundles) — nunca commitar:
.ota-secret
.DS_Store
# artefatos de deploy/build (binários grandes, geráveis — não versionar):
apk-download-rebuild/
apk-host/
releases/
email-preview.html
# backup dos ícones placeholder antigos (gerados pelo Capacitor) — substituídos pelo ícone novo:
_icons_backup_capacitor/
